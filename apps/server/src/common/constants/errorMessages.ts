import { LAST_POKEMON_DEX_NUMBER } from '@config/app';

export const ERROR_MESSAGES = {
  NOT_VALID_POKEDEX_NUMBER: `NationalPokedexNumber must be an integer between 1 and ${LAST_POKEMON_DEX_NUMBER}`,
};
