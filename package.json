{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --no-daemon",
    "lint": "turbo lint --no-daemon",
    "clean": "turbo clean",
    "prettier:format": "turbo prettier:format",
    "studio": "turbo studio",
    "ui:add": "pnpm --filter ui ui:add",
    "prisma:generate": "turbo prisma:generate",
    "db:seed": "turbo db:seed",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^20.5.9",
    "eslint": "^8.48.0",
    "husky": "^8.0.3",
    "organize-imports-cli": "^0.10.0",
    "prettier-plugin-tailwindcss": "^0.5.3",
    "prettier": "^3.0.3",
    "turbo": "latest",
    "typescript": "^5.2.2"
  },
  "packageManager": "pnpm@8.6.10"
}
